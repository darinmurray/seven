<%= form_with(model: income) do |form| %>
  <% if income.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(income.errors.count, "error") %> prohibited this income from being saved:</h2>

      <ul>
        <% income.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!--div>
    < %= form.label :user_id, style: "display: block" %>
    < %= form.text_field :user_id, value: current_account.user.id %>
  </div -->
  
  <div class="mb-3">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="mb-3">
    <%= form.label :avg_monthly_income, style: "display: block" %>
    <%= form.text_field :avg_monthly_income %>
  </div>

  <!-- div class="mb-3">
    <%= form.label :interval, style: "display: block" %>
    <%= form.number_field :interval %>
  </!-->

  <!-- div class="mb-3">
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </!-->

  <div class="form-group mb-3">
    <%= form.submit 'Save', :class => 'btn btn-primary' %>    
    <%= link_to "Cancel", user_path(current_account.user), :class => 'ms-3'  %>
  </div>
<% end %>


