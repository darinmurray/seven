<p style="color: green"><%= notice %></p>

<h2><%= @products.length %> Products in SEVEN</h2>
<p>Find me in app/views/pages/home.html.erb</p>


   
<% if rodauth.logged_in? %>
     <%= link_to "User Page", current_account.user %>
<% else %>
No on is logged in
<% end %>

<button type="button" 
        class="btn btn-lg btn-danger" 
        data-bs-toggle="popover" 
        title="Popover title" 
        data-bs-content="Amazing content, right ?">
        Click to toggle popover
</button>  

<br/><br/>
<div id="products">
  <% @products.each do |product| %>
    <%= render product %>
    <p>
      <!--  %= link_to "Show this product", product % -->
        <%= link_to "Edit(in index)", edit_product_path(product) %></li>
        <%= link_to "destroy in index", product_path(product), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
    </p>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>


