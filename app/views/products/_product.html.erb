<div id="<%= dom_id product %>">
  <span>
    <strong>Name:</strong>
    <%= product.name %>
  </span>

  <span>
    <strong>Color:</strong>
    <%= product.color %>
  </span>

  <span>
    <strong>Price:</strong>
    <%= product.price %>
  </span>

  <span>
    <strong>Sku:</strong>
    <%= product.sku %>
  </span>
  <%= link_to "Edit", edit_product_path(product) %></li>
  <%= link_to "XX", product_path(product), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</div>
